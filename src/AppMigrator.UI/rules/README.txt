Drop external rule packs here after publish.

Naming:
  anything.rule.json

Format:
  JSON array of AppRule objects using the same property names as Models/AppRule.cs

This folder is optional. The app already ships with built-in rules.
